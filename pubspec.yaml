name: flutter_application_1
description: A new Flutter project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  firebase_core_web: ^2.17.5
  fl_chart: ^0.66.2
  tflite: ^1.1.2
  cloud_firestore: ^4.17.0
  local_auth: ^2.1.7
  path_provider: ^2.1.1
  share_plus: ^7.0.1
  csv: ^5.0.3
  flutter_blue_plus: ^1.14.4 
  go_router: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  

